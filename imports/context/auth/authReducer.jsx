import {
    REGISTRO_EXITOSO,
    REGISTRO_ERROR,
    USUARIO_AUTENTICADO
} from '../../types';

const authReducer = (state, action) => {
    switch(action.type){


        default:
            return state
    }
}

export default authReducer;